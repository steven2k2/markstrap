<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Markstrap</title>
    <!-- Bootstrap CSS from CDN -->
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">
<div class="container my-5">
    <h1 class="text-center">Markstrap</h1>
    <p class="text-center">Convert Markdown to Bootstrap-styled HTML</p>

    <!-- Markdown Input Form -->
    <form action="/convert" method="POST" class="my-4">
        <div class="form-group">
            <label for="markdownText">Enter Markdown</label>
            <textarea
                    name="markdownText"
                    id="markdownText"
                    class="form-control"
                    rows="6"
                    required
            ></textarea>
        </div>
        <button type="submit" class="btn btn-primary mt-3">Convert</button>
    </form>

    <!-- Display the converted HTML -->
    <% if (content) { %>
        <h2>Bootstrap</h2>
        <div class="card mt-3">
            <div class="card-body">
                <%- content %> <!-- Outputs raw HTML content -->
            </div>
        </div>
    <% } %>
</div>
</body>
</html>